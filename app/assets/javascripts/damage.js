// Place all the behaviors and hooks related cd Dp the matching controller here.
// All this logic will automatically be available in application.js.
$('#pokemon_search').on('change', function() {
	
})